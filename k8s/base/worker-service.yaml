apiVersion: v1
kind: Service
metadata:
  name: sms-worker
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "9090"
    prometheus.io/path: "/metrics"
spec:
  selector: { app: sms-worker }
  ports:
    - name: http
      port: 9090
      targetPort: 9090
